package bg.tu_varna.sit.backend.models.enums;

//* Enum for user roles
public enum Role {
    ADMIN, //? 1 - for the entire system
    DISPATCHER, //? 28 - for each region of Bulgaria
    SCOUT //? No specific limit (the role represents ordinary people)
}
